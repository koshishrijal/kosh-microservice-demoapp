spring.application.name= ${springboot.app.name:eurekaserver}
server.port = ${server-port:8761}
eureka.instance.hostname= ${springboot.app.name:eurekaserver}
eureka.client.registerWithEureka= false
eureka.client.fetchRegistry= false
eureka.client.serviceUrl.defaultZone: http://${registry.host:localhost}:${server.port}/eureka/

